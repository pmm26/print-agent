name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: internal/webui/frontend/package-lock.json
      - name: Install dashboard dependencies
        run: npm --prefix internal/webui/frontend ci
      - name: Check dashboard
        if: runner.os == 'Linux'
        run: npm --prefix internal/webui/frontend run check
      - name: Build dashboard for embedding
        if: runner.os != 'Linux'
        run: npm --prefix internal/webui/frontend run build
      - name: Check formatting
        if: runner.os == 'Linux'
        shell: bash
        run: test -z "$(gofmt -l .)"
      - name: Vet
        run: go vet ./...
      - name: Test
        run: go test ./...
      - name: Race test
        if: runner.os == 'Linux'
        run: go test -race ./...
  vulnerabilities:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: internal/webui/frontend/package-lock.json
      - run: npm --prefix internal/webui/frontend ci
      - run: npm --prefix internal/webui/frontend run build
      - run: go install golang.org/x/vuln/cmd/govulncheck@latest
      - run: govulncheck ./...
